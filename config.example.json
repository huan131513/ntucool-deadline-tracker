{
  "canvas_base_url": "https://cool.ntu.edu.tw",

  "_comment_auth": "NTUCOOL doesn't support personal access tokens. 'chrome' mode reads your session cookie straight out of local Chrome (no password, no copy/paste) and only works when this script runs on your own machine. 'cookie' is the manual fallback. 'token' is for Canvas instances that DO support personal access tokens.",
  "canvas_auth_mode": "chrome",
  "canvas_cookie_domain": "cool.ntu.edu.tw",
  "_comment_chrome_profile": "Optional, 'chrome' mode only. browser_cookie3 only reads Chrome's \"Default\" profile unless told otherwise — if NTUCOOL is logged into a different Chrome profile, set this to that profile's folder name (e.g. 'Profile 12'; check chrome://version in that profile window for the exact name).",
  "canvas_chrome_profile": "",
  "canvas_cookie": "",
  "canvas_access_token": "",

  "_comment_telegram": "Optional. Leave both blank ('') to skip Telegram entirely — the dashboard, refresh, and CLI --list still work fine; only the notify button/hourly reminder are skipped.",
  "telegram_bot_token": "",
  "telegram_chat_id": "",

  "remind_before_days": [7, 3, 1],
  "include_courses_without_due_date": false,
  "only_active_courses": true,

  "state_file": "state.json"
}
